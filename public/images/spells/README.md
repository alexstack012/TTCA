# Spell artwork

Place optimized spell artwork here. Prefer WebP or AVIF and name each file after its spell ID, such as `acid-splash.webp`.

Use root-relative paths in the JSON:

```json
"image": "/images/spells/acid-splash.webp"
```

Angular copies everything under `public` to the root of both development and production builds.
