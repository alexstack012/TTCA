# NPC portraits

Place optimized NPC portraits here. Prefer WebP or AVIF and name each file after its entity ID, such as `ireena-kolyana.webp`.

Use root-relative paths in the JSON:

```json
"image": "/images/npcs/ireena-kolyana.webp"
```

Angular copies everything under `public` to the root of both development and production builds.
